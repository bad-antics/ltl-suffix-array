// examples/demo.ltl — runnable demo for ltl_suffix_array

import std::io
use ltl_suffix_array as lib

fn main() {
    io::println("ltl_suffix_array demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}